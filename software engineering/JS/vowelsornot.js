/*check if the variable has vowel or consonant
do it with ||
use nested loops
variable character = char
char='store in character'
check if character has a vowel, a,e,i,o,u.*/
var character = "x";
var character = character.toUpperCase();
if(character == 'A' || character == 'E' || character == 'I' || character == 'O' || character == 'U'){
    console.log(character + " This statement has vowels")
}else{
    console.log(character + " This statement is made up of constants")
}

/*if char >= 0 and <=9*/