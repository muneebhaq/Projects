/*Exercise # 1: Add the first 20 numbers of counter (from 1-20). 
Counter increments by 1. Display the sum at the end.*/
/*sum = 0;
for(let counter=1; counter<=20; counter++){
    sum = sum + counter;
}
console.log(sum);*/
let counter = 0
while(counter <=20){
        sum =0
        sum = sum + counter
        console.log(sum)
        {
            counter++
        }
}