/*Exercise # 2 : Start and end values of the loop should be determined by user. 
The loops increments by 1, 
so the start value should be lower than the end value. 
Display only even numbers.*/
/*let user1 = 20;
let user2 = 40;
for(let counter = 20; counter <=40; counter++){
    if(counter % 2 ==0)
    console.log(counter);
}*/
let user1 = 20
let user2 = 40
while(counter <= 40){
    if(counter % 2 == 0){
        console.log(counter)
        counter++
        
    }
}
