/*
let value = "1";
for(let row = 1; row<=5; row++){
    for(let col = 1; col <=3; col++){
        process.stdout.write(value.toString() + ' ');
        //converts number to a string
        value++;
    }
    console.log();
    //next line
    }
    //PRINTS NUMBER OF ROWS AND COLUMNS DISPLAYING NUMBER FOR ALL COLUMNS
*/

/*
let value = "*";
for(let row = 1; row<=5; row++){
    for(let col = 1; col <=3; col++){
        process.stdout.write(value + ' | ');
        //only for strings
        
        //value++;
        value
    }
    console.log();
    //next line
}
//PRINTS * IN 5 ROWS AND 3 COLUMNS
*/




/*let numColumn = 5
let numRow = 7
let value = 1
for(let row = 1; row <= numRow; row++){
    for(let column = 1; column <= numColumn; column++){
        process.stdout.write(row.toString() + " ")
        
    }
    console.log();
}
PRINTS ROWS AND COLUMNS DISPLAYING ROW NUMBERS
*/

/*let value = 1
let numRow = 5
let numcolumn = 7
for(let row = 1; row <= numRow; row++){
    for(let column = 1; column <= numcolumn; column++){
        process.stdout.write(row.toString() + "," + column.toString() + " ")
    }
    console.log();
}
//PRINTS ROWS AND COLUMNS DISPLAYING ROW AND COLUMN NUMBERS FOR EACH BLOCK